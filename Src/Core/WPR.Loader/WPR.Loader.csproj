<Project Sdk="Microsoft.NET.Sdk">

  <!--
    WPR.Loader — discovery / install / IL-patch / assembly-synthesis + the EF-Core
    catalogue DB (Plans/ARCHITECTURE-MIGRATION.md, Stage 2). Lower layer of the split
    from the old `WPR` core. FNA-free at the code level (FNA arrives only transitively
    via the GamerServices/Framework.Xna project refs used by the patcher/seeder).
    Types remain in `namespace WPR` (+ WPR.Models) — the split is by assembly, not
    namespace, so consumers compile unchanged.
  -->

  <PropertyGroup Condition=" '$(OS)' != 'Windows_NT' ">
    <CheckEolWorkloads>false</CheckEolWorkloads>
    <TargetFrameworks>net8.0;net8.0-android</TargetFrameworks>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(OS)' == 'Windows_NT' ">
    <CheckEolWorkloads>false</CheckEolWorkloads>
    <TargetFrameworks>net8.0-windows10.0.17763.0;net8.0-android</TargetFrameworks>
  </PropertyGroup>
  <PropertyGroup>
    <CheckEolWorkloads>false</CheckEolWorkloads>
    <SupportedOSPlatformVersion Condition="'$(TargetFramework)' == 'net8.0-android'">21.0</SupportedOSPlatformVersion>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- FFMpegCore and its System.Drawing.Common pin MOVED to WPR.Platform.Windows on 2026-08-31.
         FFMpegCore spawns an `ffmpeg` child process, so it was only ever the desktop story; it sat
         here serving AudioCompabilityConverter under a comment claiming it was "the implementation
         for all platforms", which meant Android silently transcoded nothing and every WP7 .wma
         soundtrack was mute. The transcode now goes through WPR.Engine.Audio.IAudioTranscoder
         (registry: AudioTranscoderBackend), implemented per head — FFMpegCore on Windows, the
         FFmpegKit JNI binding on Android. Do not re-add FFMpegCore here: a shared Core project is
         the one place this dependency cannot live, because it also ships it inside the APK.
         System.Drawing.Common went with it — nothing in this project uses it directly, it existed
         purely to lift FFMpegCore's transitive 5.0.2 off CVE-2021-24112. -->
    <!-- CVE-2024-43485 / GHSA-8g4q-xg66-9fp4 (High, CVSS 8.8): System.Text.Json 6.0.0-6.0.9 —
         algorithmic-complexity DoS on models with a [JsonExtensionData] property. This pin arrived
         to cover FFMpegCore 4.8.0's transitive 6.0.2, but it STAYS now that FFMpegCore has left:
         HardcodedAchievementCatalogue.cs uses System.Text.Json directly, so this is a first-party
         dependency rather than a transitive lift. The windows leg is incidentally lifted to 8.0.6
         by the vendored Projektanker.Icons.Avalonia.FontAwesome, but android does not reference
         that project, so without this reference android would resolve whatever the SDK's own
         baseline gives it. Fix landed in 8.0.5. -->
    <PackageReference Include="System.Text.Json" Version="8.0.6" />
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="$(EFVersion)" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="$(EFVersion)" />
    <!-- CVE-2025-6965 / GHSA-2m69-gcr7-jv3q (High, CVSS 7.2): the native SQLite bundled by
         SQLitePCLRaw <= 2.1.11 is affected by a memory-corruption bug (aggregate terms can exceed
         the available columns; CWE-197), fixed in SQLite 3.50.2. EF Core Sqlite pulls
         SQLitePCLRaw.bundle_e_sqlite3 2.0.4 transitively, which drags lib.e_sqlite3 with it.
         Pinned to 2.1.13, above the advisory's affected range; the bundle lifts core,
         lib.e_sqlite3 and provider.dynamic_cdecl together, so one pin covers all of them.
         Note the advisory itself lists "no patched version" — that field is stale, its affected
         range stops at 2.1.11 and the maintainer has shipped 2.1.12/2.1.13 since. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="2.1.13" />
    <PackageReference Include="Mono.Cecil" Version="0.11.4" />
    <PackageReference Include="Newtonsoft.Json" Version="13.0.1" />
    <PackageReference Include="System.Reactive" Version="5.0.0" />
    <!-- AudioTranscoderBackend moved to the engine tier (Stage 6); the installer reads the transcoder from it. -->
    <ProjectReference Include="$(SolutionDir)Engine\WPR.Engine.Audio\WPR.Engine.Audio.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The com.arthenica.ffmpegkit binding MOVED to WPR.Platform.Android on 2026-08-31, alongside
         FFMpegCore moving to WPR.Platform.Windows. It was referenced here, for the android TFM
         only, by a project that had no code able to call it — and could not have: the binding
         targeted plain net8.0, so class-parse never ran and it built an assembly with no types.
         Both transcoders now live in their own head behind IAudioTranscoder. -->
    <!-- The IAudioTranscoder contract AudioCompabilityConverter and AudioTranscoderBackend speak.
         Explicit rather than transitive-through-Framework.Silverlight: this project consumes the
         contract in its own code now. -->
    <ProjectReference Include="$(SolutionDir)\Core\WPR.Common\WPR.Common.csproj" />
    <!-- The catalogue schema (WPR.Models.ApplicationContext / Application / ApplicationType) and
         its migrations moved to Core/WPR.Database on 2026-08-30. Namespaces are unchanged, so the
         code here compiles as before — only the assembly boundary moved. -->
    <ProjectReference Include="$(SolutionDir)\Core\WPR.Database\WPR.Database.csproj" />
    <ProjectReference Include="$(SolutionDir)\Core\WPR.Framework.Silverlight\WPR.Framework.Silverlight.csproj" />
    <!-- The WPR-owned XNA type system. Needed at compile time for the two typeof(...) entries in
         ApplicationPatcher.MemberPatches (WPR.Xna.Compat.GraphicsDevice / GraphicsAdapter, the WP7
         display-mode overrides). Replaced the WPR.XnaCompability project reference when that
         project was deleted on 2026-08-29 and its last two types moved here. No cycle:
         WPR.Framework.Xna references nothing. -->
    <ProjectReference Include="$(SolutionDir)\Core\WPR.Framework.Xna\WPR.Framework.Xna.csproj" />
  </ItemGroup>

</Project>
