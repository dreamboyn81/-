<Project Sdk="Microsoft.NET.Sdk">

  <!--
    WPR.Framework.Xna — WPR-owned XNA type system (Stage 5a, Plans/ARCHITECTURE-MIGRATION.md).

    First step of severing FNA. This assembly owns the pure, backend-independent XNA VALUE/MATH
    types (vendored from FNA's MIT source, now WPR-maintained): Vector2/3/4, Matrix, Quaternion,
    Color, Rectangle, Point, Plane, Ray, BoundingBox/Sphere/Frustum, Curve*, MathHelper, their
    System.ComponentModel Design converters, and IPackedVector (which Color implements).

    Types keep their real namespaces (Microsoft.Xna.Framework[.Design][.Graphics.PackedVector]).
    FNA.Core now REFERENCES this assembly for these types (its Vector2.cs etc. were removed), so
    FNA's runtime (GraphicsDevice, SpriteBatch, …) and games share ONE value-type identity — the
    prerequisite for reimplementing the runtime behind seams in 5b/5c.

    net8.0 (NOT multi-TFM) on purpose: FNA.Core is net8.0 and references this back, so this must be
    net8.0-compatible; the net8.0-windows/android facades + consumers still consume it fine.
    No FNA/native dependency → NOT a backend leak (absent from the fitness baseline).
  -->

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <AssemblyName>WPR.Framework.Xna</AssemblyName>
    <RootNamespace>Microsoft.Xna.Framework</RootNamespace>
    <Nullable>disable</Nullable>
    <GenerateAssemblyInfo>true</GenerateAssemblyInfo>
    <!-- The relocated XNA graphics runtime (SpriteBatch/GraphicsDevice/Effect/PipelineCache/…)
         uses unsafe pointer code for vertex/state marshalling, as it did inside FNA. -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <!-- FNA's runtime (EffectParameter, SpriteBatch, …) uses internal helpers on the moved value
       types (Vector3/4.CheckForNaNs, MathHelper.MachineEpsilonFloat, etc.) that were same-assembly
       before this split. Grant FNA access so those stay internal rather than leaking to the public
       XNA surface. One-way: WPR.Framework.Xna never references FNA. -->
  <!-- The stock-effect + YUV shader blobs (used by SpriteBatch's static ctor via Resources.GetResource,
       and by the FNA VideoPlayer via IVT). Moved from FNA in 5c-1 with Resources.cs; the LogicalName MUST
       stay "Microsoft.Xna.Framework.Graphics.Effect.Resources.<name>.fxb" — that's what GetResource looks up. -->
  <ItemGroup>
    <EmbeddedResource Include="Graphics\Effect\StockEffects\FXB\AlphaTestEffect.fxb">
      <LogicalName>Microsoft.Xna.Framework.Graphics.Effect.Resources.AlphaTestEffect.fxb</LogicalName>
    </EmbeddedResource>
    <EmbeddedResource Include="Graphics\Effect\StockEffects\FXB\BasicEffect.fxb">
      <LogicalName>Microsoft.Xna.Framework.Graphics.Effect.Resources.BasicEffect.fxb</LogicalName>
    </EmbeddedResource>
    <EmbeddedResource Include="Graphics\Effect\StockEffects\FXB\DualTextureEffect.fxb">
      <LogicalName>Microsoft.Xna.Framework.Graphics.Effect.Resources.DualTextureEffect.fxb</LogicalName>
    </EmbeddedResource>
    <EmbeddedResource Include="Graphics\Effect\StockEffects\FXB\EnvironmentMapEffect.fxb">
      <LogicalName>Microsoft.Xna.Framework.Graphics.Effect.Resources.EnvironmentMapEffect.fxb</LogicalName>
    </EmbeddedResource>
    <EmbeddedResource Include="Graphics\Effect\StockEffects\FXB\SkinnedEffect.fxb">
      <LogicalName>Microsoft.Xna.Framework.Graphics.Effect.Resources.SkinnedEffect.fxb</LogicalName>
    </EmbeddedResource>
    <EmbeddedResource Include="Graphics\Effect\StockEffects\FXB\SpriteEffect.fxb">
      <LogicalName>Microsoft.Xna.Framework.Graphics.Effect.Resources.SpriteEffect.fxb</LogicalName>
    </EmbeddedResource>
    <EmbeddedResource Include="Graphics\Effect\YUVToRGBA\YUVToRGBAEffect.fxb">
      <LogicalName>Microsoft.Xna.Framework.Graphics.Effect.Resources.YUVToRGBAEffect.fxb</LogicalName>
    </EmbeddedResource>
    <!-- The audio subsystem: seams, registry and composition all live in the engine tier as of
         2026-09-01, so the framework's SoundEffect / Cue / MediaPlayer read them from there.
         One-way: WPR.Engine.Audio names no framework type, which is what the neutral
         MicrophoneInfo and System.Numerics.Vector3 in its contracts are for. -->
    <ProjectReference Include="$(SolutionDir)Engine/WPR.Engine.Audio/WPR.Engine.Audio.csproj" />
    <!-- The achievement-unlock toast (SignedInGamer). Moved out of WPR.Common on 2026-09-01. -->
    <ProjectReference Include="$(SolutionDir)Engine/WPR.Engine.Notifications/WPR.Engine.Notifications.csproj" />
  </ItemGroup>

  <!--
    GamerServices/ — absorbed from the Microsoft.Xna.Framework.GamerServices project when it was
    dissolved (2026-08-30, ApplicationPatcher.Version 19). The types keep their real
    Microsoft.Xna.Framework.GamerServices namespace; only the host assembly changed, and the
    patcher now rescopes games' GamerServices assembly refs here.

    Its one FNA-derived type, GamerServicesComponent (: GameComponent), did NOT come with them —
    it went to WPR.Backend.FNA/Compat/, exactly as GraphicsDeviceManager did at version 15. That
    is what keeps this project free of an FNA reference, which matters because FNA.Core
    references THIS project: taking FNA.Core back would be a circular reference, not merely a
    layering smell.

    The EF Core dependency is the achievements database (AchievementContext + Migrations/). It is
    the first non-trivial dependency this assembly has ever had; the XNA value/math types above
    remain untouched by it.
  -->
  <!--
    No EF Core / Sqlite / SQLitePCLRaw here any more. Stage 5e (2026-08-30) moved
    AchievementContext and its migrations into Core/WPR.Database and left behind the
    WPR.Xna.Achievements.IAchievementStore seam, which the host registers via
    XnaBackend.SetAchievements. GamerServices now states what it needs and WPR.Database supplies
    it, so the database stack no longer rides along in the assembly patched games bind.

    Re-adding a data-access package here would put EF back into every game's binding closure.
    If something in this project needs persistence, extend IAchievementStore (or add a sibling
    seam) and implement it in WPR.Database instead.
  -->
  <ItemGroup>
    <!-- Retained: the hardcoded achievement catalogues under WPR.Database/Data/Achievements are
         JSON, and GamerServices deserialises them. Pinned to 8.0.6 for CVE-2024-43485. -->
    <PackageReference Include="System.Text.Json" Version="8.0.6" />
  </ItemGroup>

  <ItemGroup>
    <!-- For Configuration / WprHostEnvironment.CurrentProductId, which is how GamerServices now
         scopes achievement rows to the running game. It used to read
         Application.Current.ProductId off the Silverlight shim, which would have dragged the
         whole Avalonia stack in here. -->
    <ProjectReference Include="$(SolutionDir)\Core\WPR.Common\WPR.Common.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Compile Update="GamerServices\Properties\Resources.Designer.cs">
      <DesignTime>True</DesignTime>
      <AutoGen>True</AutoGen>
      <DependentUpon>Resources.resx</DependentUpon>
    </Compile>
    <EmbeddedResource Update="GamerServices\Properties\Resources.resx">
      <Generator>ResXFileCodeGenerator</Generator>
      <LastGenOutput>Resources.Designer.cs</LastGenOutput>
    </EmbeddedResource>
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="FNA" />
    <!-- 5c-4: the FNA backend implements framework-internal contracts (it constructs Microphone,
         and its XACT file reader shares the moved MonoGame.Utilities.FileHelpers path helpers).
         Visibility only — WPR.Framework.Xna still references nothing. -->
    <InternalsVisibleTo Include="WPR.Backend.FNA" />
    <!-- The FAudio audio backend (Src/Audio/, split out of WPR.Backend.FNA on 2026-09-01)
         constructs Microphone and uses MonoGame.Utilities.FileHelpers for XACT bank paths. -->
    <InternalsVisibleTo Include="WPR.Audio.FAudio" />
  </ItemGroup>

</Project>
